import{useQuery}from'@tanstack/react-query';import{useLanguage}from'../../context/LanguageContext';import{apiService}from'../../services/api';export default function CategoryTabs({active,onSelect}){const{lang}=useLanguage();const{data:c}=useQuery({queryKey:['news','categories'],queryFn:()=>apiService.getNewsCategories(),staleTime:3600000});if(!c||!c.length)return null;return(<div className="flex gap-1.5 overflow-x-auto pb-2 -mx-1 px-1">{c.slice(0,12).map(cat=>(<button key={cat.id} onClick={()=>onSelect(cat.id)} className={`flex items-center gap-1.5 px-3.5 py-2 rounded-xl text-xs font-semibold whitespace-nowrap transition-all shrink-0 ${active===cat.id?'bg-[var(--color-primary)] text-white shadow-md':'bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 text-gray-600 dark:text-gray-300'}`}><span>{cat.icon}</span>{lang==='kn'?cat.labelKn:cat.label}</button>))}</div>);}