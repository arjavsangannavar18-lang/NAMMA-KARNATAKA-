import{useState,useCallback}from'react';import{useQuery,useQueryClient}from'@tanstack/react-query';import{useLanguage}from'../context/LanguageContext';import{apiService}from'../services/api';function useLS(k,fb){const[v,s]=useState(()=>{try{const x=localStorage.getItem(k);return x?JSON.parse(x):fb}catch{return fb}});const set=nv=>{s(nv);try{localStorage.setItem(k,JSON.stringify(nv))}catch{}};return[v,set]}const REGIONS=[{id:'all',i:'🗺',l:'All Districts',lk:'ಎಲ್ಲಾ ಜಿಲ್ಲೆಗಳು'},{id:'North',i:'🏜️',l:'North Karnataka',lk:'ಉತ್ತರ ಕರ್ನಾಟಕ'},{id:'South',i:'🌿',l:'South Karnataka',lk:'ದಕ್ಷಿಣ ಕರ್ನಾಟಕ'},{id:'Coastal',i:'🌊',l:'Coastal Karnataka',lk:'ಕರಾವಳಿ ಕರ್ನಾಟಕ'}];export default function DistrictsPage(){const{lang}=useLanguage();const qc=useQueryClient();const[region,setRegion]=useState('all');const[selected,setSelected]=useState(null);const[qv,setQv]=useState('');const[nearby,setNearby]=useState([]);const[det,setDet]=useState(false);const[fv,setFv]=useLS('nk-dist-fv',[]);const{data:districts,isLoading}=useQuery({queryKey:['districts'],queryFn:()=>apiService.getDistricts(),staleTime:3600000});const L=lang==='kn';const fl=districts?region==='all'?districts:districts.filter(d=>d.region===region):[];const qf=qv.trim()?districts?.filter(d=>(d.name||'').toLowerCase().includes(qv.toLowerCase())||(d.nameKn||'').includes(qv.toLowerCase())||(d.hq||'').toLowerCase().includes(qv.toLowerCase())):fl;const tfv=id=>{setFv(p=>p.includes(id)?p.filter(x=>x!==id):[...p,id])};const sd=d=>{setSelected(d);setDet(!det)};const hRef=useCallback(async()=>{await qc.invalidateQueries({queryKey:['districts']})},[qc]);useEffect(()=>{if('geolocation'in navigator){navigator.geolocation.getCurrentPosition(async p=>{const nd=await apiService.getNearbyDistricts(p.coords.latitude,p.coords.longitude);setNearby(nd||[])},()=>{})}},[apiService]);return(<div className="space-y-5 pb-4 animate-fade-in"><div className="flex items-center justify-between"><h1 className="text-xl font-bold text-gray-900 dark:text-white">🗺 {L?'ಜಿಲ್ಲೆಗಳು':'Districts'}</h1><button onClick={hRef} className="p-2 rounded-xl bg-white dark:bg-gray-800 border"><svg className="w-5 h-5 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"/></svg></button></div><div className="relative"><svg className="absolute left-3 top-1/2 -translate-y-1/2 w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/></svg><input value={qv} onChange={e=>setQv(e.target.value)} placeholder={L?'ಜಿಲ್ಲೆ ಹುಡುಕಿ...':'Search district...'} className="w-full pl-10 pr-4 py-2.5 rounded-xl bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 text-sm focus:outline-none focus:ring-2 focus:ring-[var(--color-primary)] shadow-sm"/></div><div className="flex gap-1.5 overflow-x-auto pb-2">{REGIONS.map(r=>(<button key={r.id} onClick={()=>setRegion(r.id)} className={`flex items-center gap-1 px-3 py-1.5 rounded-xl text-xs font-semibold whitespace-nowrap shrink-0 ${region===r.id?'bg-[var(--color-primary)] text-white':'bg-white dark:bg-gray-800 border text-gray-600 dark:text-gray-300'}`}>{r.i} {L?r.lk:r.l}</button>))}</div>{isLoading?<div className="flex justify-center py-8"><div className="loading-spinner"/></div>:<div><p className="text-xs text-gray-400 mb-2">{qf.length} {L?'ಜಿಲ್ಲೆಗಳು':'districts'}</p><div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3">{qf.map(d=>(<button key={d.id} onClick={()=>sd(d)} className="text-left p-4 rounded-2xl bg-white dark:bg-gray-800 border hover:border-[var(--color-primary)] hover:shadow-md transition-all group"><div className="flex items-start justify-between mb-2"><h3 className="text-sm font-bold group-hover:text-[var(--color-primary)]">{L&&d.nameKn?d.nameKn:d.name}</h3><button onClick={e=>{e.stopPropagation();tfv(d.id)}} className="text-sm">{fv.includes(d.id)?'⭐':'☆'}</button></div>{d.hq&&<p className="text-xs text-gray-500 flex items-center gap-1">🏛️ {d.hq}</p>}{d.taluks&&<p className="text-xs text-gray-400 mt-1">📌 {d.taluks} {L?'ತಾಲೂಕುಗಳು':'taluks'}{d.villages?' · '+d.villages+' '+(L?'ಹಳ್ಳಿಗಳು':'villages'):''}</p>}{d.knownFor&&<p className="text-xs text-gray-400 mt-1">🌟 {L&&d.knownForKn?d.knownForKn:d.knownFor}</p>}{d.area&&<p className="text-[10px] text-gray-400">{d.area} km² · {(d.pop/100000).toFixed(1)}L</p>}</button>))}</div></div>}{selected&&(<div className="fixed inset-0 z-50 flex items-center justify-center bg-black/50 p-4" onClick={()=>setSelected(null)}><div className="bg-white dark:bg-gray-800 rounded-2xl shadow-xl max-w-md w-full max-h-[80vh] overflow-y-auto p-6" onClick={e=>e.stopPropagation()}><div className="flex items-center justify-between mb-4"><h2 className="text-lg font-bold">{L&&selected.nameKn?selected.nameKn:selected.name}</h2><button onClick={()=>setSelected(null)} className="text-gray-400 text-xl">✕</button></div>{selected.hq&&<p className="text-sm mb-2"><span className="font-semibold">{L?'ಮುಖ್ಯಸ್ಥಳ':'HQ'}:</span> {selected.hq}</p>}{selected.taluks&&<p className="text-sm mb-2"><span className="font-semibold">{L?'ತಾಲೂಕುಗಳು':'Taluks'}:</span> {selected.taluks}</p>}{selected.villages&&<p className="text-sm mb-2"><span className="font-semibold">{L:'ಹಳ್ಳಿಗಳು':'Villages'}:</span> {selected.villages}</p>}{selected.knownFor&&<p className="text-sm mb-2"><span className="font-semibold">{L:'ಪ್ರಸಿದ್ಧ':'Known For'}:</span> {L&&selected.knownForKn?selected.knownForKn:selected.knownFor}</p>}{selected.area&&<p className="text-sm mb-2"><span className="font-semibold">📐 Area:</span> {selected.area} km²</p>}{selected.pop&&<p className="text-sm mb-2"><span className="font-semibold">👥 Population:</span> {(selected.pop/100000).toFixed(1)}L</p>}{selected.region&&<p className="text-sm mb-2"><span className="font-semibold">{L:'ವಿಭಾಗ':'Region'}:</span> {selected.region}</p>}</div></div>)}<p className="text-center text-xs text-gray-400 dark:text-gray-500 pt-2">{L?'31 ಜಿಲ್ಲೆಗಳು · ನಾಲ್ಕು ವಿಭಾಗಗಳು. 2011 ಜನಗಣತಿ.':'31 districts · 4 divisions. Census 2011 data.'}</p></div>)}