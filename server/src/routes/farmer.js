import{Router}from'express';import{CATEGORIES,CROPS,CROP_CALENDAR,SCHEMES,KVKS,HELPLINES,searchFarmer}from'../services/farmerService.js';const r=Router();r.get('/categories',(_,res)=>res.json(CATEGORIES));r.get('/crops',(_,res)=>res.json(CROPS));r.get('/calendar',(_,res)=>res.json(CROP_CALENDAR));r.get('/schemes',(_,res)=>res.json(SCHEMES));r.get('/kvks',(_,res)=>res.json(KVKS));r.get('/helplines',(_,res)=>res.json(HELPLINES));r.get('/search',(req,res)=>res.json(searchFarmer(req.query.q||'')));r.get('/',(_,res)=>res.json({categories:CATEGORIES,crops:CROPS.length,schemes:SCHEMES.length,kvks:KVKS.length}));export default r;